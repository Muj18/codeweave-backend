### IaC State / Concurrency (Terraform)
- Inspect `terraform state list`; verify lock table; never force-unlock without checking active runs.
- Backend sanity (S3/GCS/Blob + lock table). Serialize applies per env (CI concurrency).
- Policy: pre-merge checks for probes/resources/secrets.
