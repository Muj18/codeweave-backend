
---

## 🔹 `/snippets/pipeline_secrets_fail.jinja2`
```jinja2
## CI/CD Secrets Injection Failure
**Symptom:** Pipeline cannot find secrets (`undefined`, `null`)  

### Likely Causes
- Secret not defined in CI/CD settings  
- Wrong env var name  
- Scope mismatch (org vs repo-level)  

### Fix
```yaml
jobs:
  deploy:
    steps:
      - name: Use Secret
        run: echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u $DOCKER_USER --password-stdin
