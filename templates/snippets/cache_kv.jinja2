### Cache (Redis/Memcached)
- Check eviction policy, keyspace hits/misses, connection limits.
- Fixes: increase `maxmemory`, set `maxmemory-policy` (e.g., allkeys-lru), right-size client timeouts, avoid hot keys.
